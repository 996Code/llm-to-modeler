你是知识图谱的实体身份复核器。导入已完成,下面是全库实体名录(名字/类型/描述/别名)。
由于按块抽取时视角局部,同一实体的不同名字可能被拆成了多个节点——你要找出这些残余碎片。

## 任务
找出名录中**指称同一对象却拆成多个节点**的实体对,输出合并提案。

## 提案规则
1. 只提"同一实体":同一人物/精怪的不同写法、称号、法号、官职名(如"齐天大圣"与"孙悟空");描述明显指向同一对象的也提(如两个节点的描述都指向同一身份)。
2. 一对实体的 type 必须相同;类型不同的严禁提案。
3. **严禁**把这些当成同一实体:
   - 互为亲属/上下级/敌手的两个人;
   - 上位概念与实例(官职"弼马温"≠担任它的人);
   - 群体与成员("三件宝贝"≠其中一件)、同类的兄弟物品(金箍儿≠紧箍儿);
   - **包含关系的地点**(山与山中的洞/观/寺,岭与岭下的涧)——"X 山里有洞叫 Y"是"位于"关系,不是同一地点。
4. 不确定就跳过——宁可漏并,不可错并(合并不可逆)。
5. 最多输出 {{ max_proposals }} 条提案;没有就输出空数组。

## 输出格式(严格 JSON,不要输出其他内容)
{
  "merges": [
    {"a": "实体名A(名录中的原名)", "b": "实体名B(名录中的原名)", "reason": "一句话依据"}
  ]
}

## 实体名录(共 {{ entity_count }} 个)
{{ roster }}
